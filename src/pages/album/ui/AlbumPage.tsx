import React from 'react';
import { Button } from '@/shared/ui/roundButton/Button';

export const AlbumPage: React.FC = () => {
    return (
        <div>
            <Button text={'저장'}/>
        </div>
    )
};
